# Members With Expired Subscription

## A small Joomla 5.XX site module that displays a list of members with expired subscriptions, filtered by plan.
Uses Joomla CB Subscriptions component tables. Plan must be selected when configuring module. 
